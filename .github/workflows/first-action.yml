name: First Workflow
on: workflow_dispatch
jobs: 
  first-job: 
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: 
          echo "Hello World"
          echo "How are you"
          echo "this is my first attempt to work with github actions"
      - name: Print Goodbye
        run: echo "Done - bye!"
